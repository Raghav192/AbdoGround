# Install torch CPU build first (smaller, no CUDA needed for inference):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# then: pip install -r requirements.txt
torch
torchvision
fastapi
uvicorn[standard]
pillow
numpy
matplotlib
python-multipart